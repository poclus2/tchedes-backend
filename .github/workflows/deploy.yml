name: Deploy Backend to Contabo (GHCR)

on:
  push:
    branches:
      - main
      - master

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and push Backend image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:main
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push OCR image
        uses: docker/build-push-action@v4
        with:
          context: ./python-ocr
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/tchedes-ocr:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            mkdir -p /opt/tchedes-backend
            cd /opt/tchedes-backend
            
            # Create the full docker-compose.yml for production
            cat <<EOF > docker-compose.yml
            version: '3.8'
            services:
              tchedes-api:
                image: ghcr.io/${{ github.repository }}:main
                container_name: tchedes-api
                restart: always
                ports:
                  - "3015:3000"
                environment:
                  - NODE_ENV=production
                  - FRONTEND_URL=https://app-tchedes.dartsia.app
                  - PORT=3000
                  - DATABASE_URL=postgresql://tchedes_user:tchedes_secure_pass_2026@tchedes-db:5432/tchedes_db?schema=public
                  - REDIS_URL=redis://tchedes-redis:6379
                  - REDIS_HOST=tchedes-redis
                  - JWT_SECRET=\${JWT_SECRET:-tchedes_super_secret_jwt_key_prod}
                  - CORS_ORIGIN=https://tchedes.dartsia.app,https://app-tchedes.dartsia.app,https://dashboard.dartsia.app
                  - GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}
                depends_on:
                  - tchedes-db
                  - tchedes-redis
                volumes:
                  - uploads_data:/app/uploads

              tchedes-worker:
                image: ghcr.io/${{ github.repository }}:main
                container_name: tchedes-worker
                restart: always
                command: npm run worker
                environment:
                  - NODE_ENV=production
                  - FRONTEND_URL=https://app-tchedes.dartsia.app
                  - DATABASE_URL=postgresql://tchedes_user:tchedes_secure_pass_2026@tchedes-db:5432/tchedes_db?schema=public
                  - REDIS_URL=redis://tchedes-redis:6379
                  - REDIS_HOST=tchedes-redis
                  - OCR_SERVICE_URL=http://python-ocr:8000
                  - GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}
                depends_on:
                  - tchedes-db
                  - tchedes-redis
                  - python-ocr
                volumes:
                  - uploads_data:/app/uploads

              python-ocr:
                image: ghcr.io/${{ github.repository_owner }}/tchedes-ocr:latest
                container_name: python-ocr
                restart: always
                volumes:
                  - uploads_data:/app/uploads

              tchedes-db:
                image: postgres:16-alpine
                container_name: tchedes-db
                restart: always
                environment:
                  - POSTGRES_USER=tchedes_user
                  - POSTGRES_PASSWORD=tchedes_secure_pass_2026
                  - POSTGRES_DB=tchedes_db
                volumes:
                  - pgdata:/var/lib/postgresql/data

              tchedes-redis:
                image: redis:7-alpine
                container_name: tchedes-redis
                restart: always
                volumes:
                  - redisdata:/data

            volumes:
              pgdata:
              redisdata:
              uploads_data:
            EOF
            
            docker compose pull
            docker compose up -d
            echo "Successfully deployed Backend and OCR to Contabo! 🚀"
